<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <title>Express WebApp</title>
    </head>
    <body>
        <h1><%= title %> on the server</h1>
        <ul>
            <% for(var i=0; i<list.length; i++) { %>
                <li><%= list[i] %></li>
            <% } %>
        </ul>
    </body>
</html>
